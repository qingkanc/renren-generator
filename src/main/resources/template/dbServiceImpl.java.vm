package ${package}.${moduleName}.service;

import org.springframework.stereotype.Service;
import com.social.credits.finance.base.data.service.BaseDbServiceImpl;
import ${package}.${moduleName}.model.${className};

/**
 *
 * @author 代码生成器
 * @Description ${comments}
 * @date ${datetime}
 */
@Service
public class ${className}DbServiceImpl extends BaseDbServiceImpl<${className}> implements ${className}DbService {

}